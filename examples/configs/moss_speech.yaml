# Single A800-80GB: BF16 AR/KV, FP32 codec, TP=1, eager execution.
# Override assets directly with --model-path, --codec-path and --voice-wav.
# Prompt/output <=512 grid rows, context1024, KV4096, AR running4/waiting20.
config_cls: MossSpeechPipelineConfig
model_path: models/MOSS-Speech
